import React, { useState, useEffect } from "react";



function FlowChart(project) {
    // course scheduling algorithm to figure out where to place different tasks (task with prere and task without prere)
    /*
        automatically generate stuff based on list view input, async function and put it into another collection (projectFlowChart)
        sotrage: {
            edges: [ { startNodeId: endNodeId }, ... ],
            nodes: [ { nodeId(taskId): bool (isFinished), ... } ],

        }
    */

};


export default FlowChart;